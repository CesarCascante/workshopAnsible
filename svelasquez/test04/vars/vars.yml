output_path: "/tmp/reporte.csv"
asunto: "Reporte de servidor"
mensaje: "Adjunto encontrarás el reporte generado automáticamente {{ ansible_date_time.date }}"
smtp_host: "smtp.office365.com"
smtp_port: "587"
mail_from: "GSSO@GBM.NET"
smtp_user: "{{ user_mailserver | default(omit) }}"
smtp_pass: "{{ passwd_mailserver | default(omit) }}"