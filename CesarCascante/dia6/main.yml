---
- name: llamado a role windows en codigo.
  hosts: Windows
  vars: 
    - ansible_user: "{{ user_win_server }}"
    - ansible_password: "{{ passwd_win_server }}"
  roles:
    - windows

- name: llamado a role rhel en codigo.
  hosts: Linux
  roles:
    - rhel